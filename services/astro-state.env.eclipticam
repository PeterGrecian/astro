# /etc/default/astro-state on the eclipticam Pi.
# Cameras assigned to this host's state daemon (both v1 and v3w share
# the Pi and the same location.json, but each writes its own state.json).
CAMERAS="--camera eclipticam-v1 --camera eclipticam-v3w"
